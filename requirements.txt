# Perception & AI
ultralytics          # For YOLO object detection
opencv-python        # For image processing and UI windows
numpy                # For lidar data calculations
ollama               # For Moondream VLM decision layer

# ROS 2 & Middleware (Note: these usually come with ROS 2 Humble)
# rclpy              # ROS 2 Python client (System-level)
# mavros_msgs        # MAVROS message definitions (System-level)

# Utilities
protobuf==4.21.12    # Specifically for Gazebo transport message handling
